from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Using latent space decompositions as input to bayesian optimization exploration methods',
    author='Robert Tang-Kong',
    license='MIT',
)
